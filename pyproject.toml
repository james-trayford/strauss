[build-system]
requires = [
    "ffmpeg-python",
    "matplotlib",
    "numpy>=1.22",
    "pandas",
    "pychord",	
    "scipy",
    "setuptools>=42",
    "tqdm",
    "wavio",
    "wheel"
]
build-backend = "setuptools.build_meta"